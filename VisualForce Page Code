<apex:page controller="VFpageApexClassToCreateContact">
    <apex:form >
        <apex:pageMessages />

        <apex:pageBlock title="Create New Contact">
            
            <apex:pageBlockSection columns="1">

                <apex:selectList value="{!selectedAccountId}" size="1" label="Select Account">
                    <apex:selectOptions value="{!accountOptions}" />
                </apex:selectList>

                <apex:inputText value="{!firstName}" label="First Name"/>
                <apex:inputText value="{!lastName}" label="Last Name"/>
                <apex:inputText value="{!email}" label="Email"/>
                <apex:inputText value="{!phone}" label="Phone"/>

            </apex:pageBlockSection>

            <apex:pageBlockButtons >
                <apex:commandButton value="Save Contact" action="{!saveContact}" />
            </apex:pageBlockButtons>
            
        </apex:pageBlock>
    </apex:form>
</apex:page>
